{
  "name": "polarbear-vocab",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "workspaces": [
    "app-ui"
  ],
  "scripts": {
    "lexicon:build": "cargo run -p polarbear-lexicon-builder -- data/source data/generated/content.db",
    "ios:sync-icons": "node scripts/sync-ios-icons.mjs",
    "translation-model:verify": "node scripts/verify-translation-model.mjs",
    "dev": "pnpm run lexicon:build && pnpm run translation-model:verify && pnpm run ios:sync-icons && pnpm --filter @polarbear/vocab-ui run dev",
    "build": "pnpm run lexicon:build && pnpm run translation-model:verify && pnpm run ios:sync-icons && pnpm --filter @polarbear/vocab-ui run build",
    "typecheck": "pnpm --filter @polarbear/vocab-ui run typecheck",
    "test": "cargo test --workspace && pnpm --filter @polarbear/vocab-ui run test",
    "test:visual": "playwright test --config tests/visual/playwright.config.ts",
    "check": "pnpm run typecheck && pnpm run test && cargo fmt --all --check && cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@tauri-apps/cli": "^2.0.0"
  }
}
